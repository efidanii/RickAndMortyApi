import pickleError from "./pickleError.gif";
export const Error = () => {
  return (
    <img
      src={pickleError}
      alt=""
      style={{ width: 190 + "px", margin: 0 + " auto" }}
    />
  );
};
